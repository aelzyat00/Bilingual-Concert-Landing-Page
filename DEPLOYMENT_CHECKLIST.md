# ✅ قائمة التحقق قبل النشر

## 🔍 قبل النشر المحلي

### المتطلبات الأساسية
- [ ] Node.js مثبت (18+)
- [ ] npm/pnpm/yarn مثبت
- [ ] حساب Supabase نشط
- [ ] Git مثبت

### البيئة
- [ ] المتغيرات في `.env`:
  ```
  VITE_SUPABASE_URL=...
  VITE_SUPABASE_ANON_KEY=...
  ```
- [ ] تم نسخ المتغيرات بشكل صحيح
- [ ] بدون مسافات إضافية

---

## 🗄️ قاعدة البيانات

### إعداد Supabase
- [ ] حساب Supabase منشأ
- [ ] Project جديد
- [ ] الاتصال بـ Console

### تنفيذ SQL
- [ ] افتح SQL Editor
- [ ] شغّل `01-create-theater-tables.sql`
  ```
  ✓ تم إنشاء theater_seats
  ✓ تم إنشاء bookings
  ✓ تم إنشاء booking_seats
  ```
- [ ] شغّل `02-seed-theater-seats.sql`
  ```
  ✓ تم إدراج 277 مقعد
  ✓ تم توزيعها على VIP/Classic
  ```

### التحقق من البيانات
```sql
-- شغّل هذه الاستعلامات:
SELECT COUNT(*) FROM theater_seats; -- يجب = 277
SELECT COUNT(*) FROM theater_seats WHERE theater_type = 'vip'; -- يجب = 23
SELECT COUNT(*) FROM theater_seats WHERE theater_type = 'classic'; -- يجب = 254
```

---

## 📥 تثبيت المشروع

### Clone المشروع
```bash
git clone [your-repo-url]
cd Bilingual-Concert-Landing-Page
```

### تثبيت المكتبات
```bash
npm install
# أو
pnpm install
# أو
yarn install
```

### التحقق من المكتبات
- [ ] `@supabase/supabase-js` مثبت
- [ ] `motion` (Framer Motion) مثبت
- [ ] `sonner` مثبت
- [ ] `lucide-react` مثبت

---

## 🧪 الاختبار المحلي

### تشغيل التطبيق
```bash
npm run dev
```

### التحقق الأساسي
- [ ] الصفحة تحمل بدون أخطاء
- [ ] الواجهة ظاهرة بشكل صحيح
- [ ] الألوان صحيحة (ذهبي/أحمر)

### اختبار الحجز
- [ ] انقر "احجز الآن"
- [ ] اختر VIP
- [ ] اختر كمية
- [ ] المقاعد تحمل من DB
- [ ] اختر مقاعد
- [ ] أكمل جميع المراحل
- [ ] تحقق من رسالة النجاح

### التحقق من قاعدة البيانات
```sql
-- يجب أن تظهر البيانات المحفوظة:
SELECT * FROM bookings ORDER BY created_at DESC LIMIT 1;
SELECT * FROM booking_seats WHERE booking_id = '...';
```

---

## 🐛 التصحيح المحلي

### في حال الأخطاء:

**خطأ: "VITE_SUPABASE_URL is not defined"**
```
✓ تحقق من .env
✓ أعد تشغيل dev server
✓ افتح Console (F12)
```

**خطأ: "No data from theater_seats"**
```
✓ تحقق من تنفيذ SQL
✓ تأكد من وجود البيانات في DB
✓ تحقق من RLS policies
```

**خطأ: "Blank page after input"**
```
✓ افتح Developer Tools (F12)
✓ شغّل Console logs
✓ انظر للأخطاء
```

---

## 📱 اختبار الريسبونسيف

### الأجهزة
- [ ] Desktop (1920x1080)
- [ ] Laptop (1366x768)
- [ ] iPad (768x1024)
- [ ] iPhone (375x667)

### المتصفحات
- [ ] Chrome (الإصدار الأخير)
- [ ] Firefox (الإصدار الأخير)
- [ ] Safari (الإصدار الأخير)
- [ ] Edge (الإصدار الأخير)

### الميزات
- [ ] العربية (RTL) تعمل
- [ ] الإنجليزية (LTR) تعمل
- [ ] المقاعد تظهر بشكل صحيح
- [ ] الزر reactiv
- [ ] الأنيميشنات سلسة

---

## 🚀 النشر على Vercel

### الخطوة 1: Push للـ GitHub
```bash
git add .
git commit -m "Cinema booking system v2"
git push origin main
```

### الخطوة 2: ربط Vercel
- [ ] ادخل https://vercel.com
- [ ] اختر "Import Project"
- [ ] اختر GitHub repository
- [ ] أكمل الخطوات

### الخطوة 3: متغيرات البيئة
في Vercel Dashboard:
```
Settings → Environment Variables
```

أضف:
```
VITE_SUPABASE_URL = ...
VITE_SUPABASE_ANON_KEY = ...
```

### الخطوة 4: النشر
- [ ] انقر "Deploy"
- [ ] انتظر البناء (2-5 دقائق)
- [ ] تحقق من الرابط

---

## ✅ بعد النشر

### التحقق على الويب
- [ ] الموقع يحمل بدون أخطاء
- [ ] الوظائف كاملة
- [ ] قاعدة البيانات متصلة
- [ ] التذاكر تحمل

### الأداء
- [ ] الصفحة تحمل < 3 ثواني
- [ ] لا توجد أخطاء في Console
- [ ] الأنيميشنات سلسة

### الأمان
- [ ] جميع البيانات مشفرة
- [ ] لا توجد keys في الكود
- [ ] HTTPS فعّال

---

## 📊 مراقبة الإنتاج

### تتبع الأخطاء
```bash
# في Vercel:
Logs → Edge Logs
# ابحث عن [v0] errors
```

### قاعدة البيانات
```sql
-- تحقق يومي من الحجوزات:
SELECT COUNT(*) FROM bookings WHERE DATE(created_at) = TODAY();
SELECT COUNT(*) FROM bookings WHERE status = 'completed';
SELECT COUNT(*) FROM bookings WHERE status = 'pending';
```

### النسخ الاحتياطي
```bash
# تأكد من النسخ الاحتياطي:
Supabase → Database → Backups
```

---

## 🎯 المرحلة النهائية

### التوثيق
- [ ] وثّق أي تغييرات
- [ ] أضف ملاحظات الإصدار
- [ ] حدّث README

### الإعلان
- [ ] شارك الرابط
- [ ] أرسل بريد للعملاء
- [ ] انشر على وسائل التواصل

### الدعم
- [ ] جهّز فريق الدعم
- [ ] أنشئ قائمة الأسئلة الشائعة
- [ ] حدّد قنوات التواصل

---

## 🚨 حالات الطوارئ

### الموقع معطل؟
```bash
# الخطوات:
1. تحقق من Vercel status
2. تحقق من قاعدة البيانات
3. تحقق من المتغيرات
4. أعد النشر
```

### قاعدة البيانات معطلة؟
```bash
# الخطوات:
1. تحقق من اتصال Supabase
2. تحقق من RLS policies
3. اتصل بـ Supabase support
4. استعد من النسخة الاحتياطية
```

### فقدان البيانات؟
```bash
# الخطوات:
1. توقف عن الكتابة فوراً
2. استعد من النسخة الاحتياطية
3. تحقق من السجلات
4. اتصل بـ Supabase
```

---

## 📋 النموذج النهائي

```
✅ المتطلبات الأساسية
✅ قاعدة البيانات
✅ التثبيت المحلي
✅ الاختبار المحلي
✅ الاختبار الريسبونسيف
✅ النشر على Vercel
✅ التحقق على الويب
✅ المراقبة المستمرة

🎉 READY FOR PRODUCTION!
```

---

## 📞 للمساعدة

| المشكلة | الحل | الوقت |
|--------|-----|-------|
| خطأ البناء | تحقق من npm install | 5 دقائق |
| قاعدة بيانات | تحقق من SQL | 10 دقائق |
| الأداء | حسّن صور | 15 دقيقة |
| أخطاء وقت التشغيل | افتح Console | 20 دقيقة |

---

**آخر تحديث**: 2026-02-28
**الحالة**: ✅ جاهز للإنتاج

الموقع الآن جاهز للإطلاق! 🚀
