# دليل الاختبار الشامل

## 🧪 اختبار النظام الكامل

### قبل البدء:
- ✅ تأكد من تثبيت المتغيرات (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`)
- ✅ تأكد من تنفيذ SQL scripts
- ✅ جميع المكتبات مثبتة

---

## 1. اختبار المرحلة 1: اختيار النوع

```
✓ انقر على زر "احجز الآن"
✓ تأكد من ظهور نافذة الحجز
✓ اختر "VIP" أو "Classic"
✓ يجب الانتقال للمرحلة 2
```

---

## 2. اختبار المرحلة 2: اختيار الكمية

```
✓ اختر الكمية من 1-10
✓ تأكد من تحديث الرقم
✓ حاول إدخال رقم أكبر من 10 (يجب أن ينسحب)
✓ انقر "التالي"
```

---

## 3. اختبار المرحلة 3: اختيار المقاعد

```
✓ يجب أن ترى شاشة اختيار المقاعد
✓ الخريطة تحميل من قاعدة البيانات
✓ انقر على مقاعد متعددة (حسب الكمية المختارة)
✓ المقاعد المختارة تتوهج بالذهب
✓ الملخص يحدث فوراً
✓ انقر "التالي"
```

---

## 4. اختبار المرحلة 4: البيانات الشخصية

```
✓ أدخل الاسم (يجب أن يكون 2+ أحرف)
✓ أدخل رقم الهاتف (صيغة: 201012345678)
✓ أدخل البريد الإلكتروني (اختياري)
✓ تحقق من رسائل الخطأ:
  - اسم فارغ → خطأ
  - رقم خاطئ → خطأ
  - بريد خاطئ → خطأ
✓ انقر "التالي"
```

---

## 5. اختبار المرحلة 5: الدفع

```
✓ اختر طريقة الدفع (Vodafone/InstaPay/Card)
✓ حاول رفع صورة (اختيارية)
- نسق الملف الصحيح: PNG/JPG
- الحد الأقصى: 5MB
✓ انقر "التالي"
```

---

## 6. اختبار المرحلة 6: التأكيد

```
✓ عرض معرف الحجز (Booking ID)
✓ انسخ المعرف (Copy button)
✓ انقر "تحميل التذاكر"
  - يجب تحميل ملفات منفصلة لكل مقعد
  - أسماء الملفات: Ticket-{ID}-A1.png
✓ شارك عبر WhatsApp
```

---

## 7. اختبار قاعدة البيانات

```sql
-- تحقق من الحجز:
SELECT * FROM bookings WHERE guest_name = 'اسم الزائر';

-- تحقق من المقاعد المحجوزة:
SELECT * FROM booking_seats WHERE booking_id = '...';

-- تحقق من توفر المقاعد:
SELECT * FROM theater_seats WHERE is_available = false;
```

---

## 8. اختبار الأخطاء

### اختبر الحالات الخاصة:

```
1. حاول الحجز بدون إنترنت
   ✓ يجب ظهور رسالة خطأ واضحة

2. حاول الحجز بـ booking ID مكرر
   ✓ النظام ينشئ ID فريد دائماً

3. حاول اختيار مقاعد محجوزة
   ✓ يجب أن تكون معطلة (disabled)

4. أغلق النافذة وأعد الفتح
   ✓ البيانات لا تُفقد إذا لم تكمل
```

---

## 9. اختبار الأداء

```
✓ تحميل الصفحة < 2 ثانية
✓ تحميل خريطة المقاعد < 1 ثانية
✓ تحميل التذاكر < 5 ثواني
✓ بدون تأخير في الأنيميشنات
```

---

## 10. اختبار التوافقية

### الأجهزة:
- [ ] iPhone (iOS)
- [ ] Android
- [ ] iPad
- [ ] Desktop (Chrome)
- [ ] Desktop (Firefox)
- [ ] Desktop (Safari)

### اللغات:
- [ ] العربية (RTL)
- [ ] الإنجليزية (LTR)

---

## نتائج الاختبار

| المرحلة | ✓ / ✗ | ملاحظات |
|--------|------|--------|
| النوع | | |
| الكمية | | |
| المقاعد | | |
| البيانات | | |
| الدفع | | |
| التأكيد | | |
| قاعدة البيانات | | |
| الأخطاء | | |
| الأداء | | |

---

## أكوام الأخطاء الشائعة

```
❌ "Cannot read property 'theater_type' of null"
   ← تأكد من تحميل قاعدة البيانات

❌ "Supabase environment variables not configured"
   ← أضف المتغيرات إلى .env

❌ "Seats not loading"
   ← تحقق من تنفيذ SQL scripts

❌ "Downloads not working"
   ← تأكد من Canvas API support
```

---

## 🎉 عند النجاح الكامل:
```
✅ جميع المراحل تعمل
✅ قاعدة البيانات محفوظة
✅ التذاكر تحمل بنجاح
✅ الأخطاء معالجة بشكل صحيح
✅ النظام جاهز للإنتاج!
```
