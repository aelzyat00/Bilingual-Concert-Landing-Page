# نظام حجز تذاكر السينما - دليل الإعداد الكامل

## تم إعداد النظام بنجاح ✅

### متغيرات البيئة المُضافة:
```
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

---

## 1️⃣ البنية الشاملة للنظام

### قاعدة البيانات (Supabase)

#### جدول `theater_seats`
```sql
- id: UUID (المعرف الفريد)
- theater_type: 'vip' | 'classic'
- row: VARCHAR (A, B, C, ...L)
- seat_number: INTEGER (1-30)
- is_available: BOOLEAN (متاح/غير متاح)
- created_at: TIMESTAMP
- updated_at: TIMESTAMP
```

#### جدول `bookings`
```sql
- id: UUID
- booking_id: VARCHAR (RT-...-...)
- guest_name: VARCHAR
- phone: VARCHAR
- email: VARCHAR
- theater_type: 'vip' | 'classic'
- payment_method: VARCHAR
- receipt_url: TEXT
- status: 'pending' | 'completed' | 'cancelled'
- created_at: TIMESTAMP
- updated_at: TIMESTAMP
```

#### جدول `booking_seats`
```sql
- id: UUID
- booking_id: UUID (FK → bookings.id)
- seat_id: UUID (FK → theater_seats.id)
- row: VARCHAR
- seat_number: INTEGER
- created_at: TIMESTAMP
```

---

## 2️⃣ مراحل الحجز (6 مراحل)

### المرحلة 1: اختيار نوع التذكرة
- **VIP**: 23 تذكرة (صفوف: A/B/C)
  - الألوان: ذهبي (#C6A04C)
- **Classic**: 254 تذكرة (صفوف: A-L)
  - الألوان: أحمر (#A8382A)

### المرحلة 2: اختيار الكمية
- الحد الأدنى: 1 تذكرة
- الحد الأقصى: 10 تذاكر
- العدد يتغير تلقائياً حسب المقاعد المتاحة

### المرحلة 3: اختيار المقاعد
- عرض مباشر من قاعدة البيانات
- المقاعد المحجوزة تظهر بلون أحمر
- المقاعد المتاحة تظهر بلون فاتح
- تحديد المقاعد المختارة بلون الذهب/الأحمر

### المرحلة 4: البيانات الشخصية
- الاسم (إلزامي، 2+ أحرف)
- رقم الهاتف (إلزامي، صيغة مصرية: 01XXXXXXXXX)
- البريد الإلكتروني (اختياري)

### المرحلة 5: طريقة الدفع
- **Vodafone Cash** (015/011/012/010)
- **InstaPay** (للتحويل البنكي)
- **Debit/Credit Card**
- رفع إيصال الدفع (صورة)

### المرحلة 6: التأكيد والتحميل
- عرض معرف الحجز (Booking ID)
- زر تحميل التذاكر (PDF منفصلة لكل مقعد)
- مشاركة عبر WhatsApp

---

## 3️⃣ نظام التذاكر

### اسم الملف:
```
Ticket-{bookingId}-{row}{seatNumber}.png
مثال: Ticket-RT1706123456ABC-A1.png
```

### محتوى كل تذكرة:
```
┌─────────────────────────────────┐
│  CINEMA EVENT 2026              │
│  [VIP/CLASSIC TICKET]           │
├─────────────────────────────────┤
│ GUEST NAME: [اسم الزائر]         │
│ BOOKING ID: RT-...-...          │
│ SEAT: A1                        │
│ PRICE: [السعر]                  │
├─────────────────────────────────┤
│   BARCODE: ██░███░░█░░░...      │
└─────────────────────────────────┘
```

---

## 4️⃣ الملفات المُنشأة

### الخدمات (`src/app/services/`)
```
├── supabase-client.ts          # اتصال Supabase
├── booking-service.ts          # عمليات قاعدة البيانات
└── ticket-generator.ts         # توليد التذاكر
```

### المكونات (`src/app/components/`)
```
├── booking-flow-v2.tsx         # النظام الرئيسي الجديد
├── seat-picker.tsx             # اختيار المقاعد
└── seat-layout.ts              # تعريف تخطيط المقاعد
```

### قاعدة البيانات (`scripts/`)
```
├── 01-create-theater-tables.sql # إنشاء الجداول
└── 02-seed-theater-seats.sql   # ملء المقاعد
```

---

## 5️⃣ كيفية الاستخدام

### أ) للمستخدم النهائي:

1. **انقر على "احجز الآن"**
2. **اختر نوع التذكرة** (VIP أو Classic)
3. **حدد الكمية** (1-10)
4. **اختر المقاعد** من الخريطة
5. **أدخل بيانات التواصل**
6. **اختر طريقة الدفع** ورفع الإيصال
7. **انقر تأكيد** وحمّل التذاكر

### ب) للمطورين:

```typescript
// استيراد الخدمات
import { getAvailableSeats, createBooking } from '@/services/booking-service';
import { downloadMultipleTickets } from '@/services/ticket-generator';

// الحصول على المقاعد المتاحة
const seats = await getAvailableSeats('vip');

// إنشاء حجز
const bookingId = await createBooking({
  name: 'أحمد محمد',
  phone: '201012345678',
  email: 'user@example.com',
  theater_type: 'vip',
  seats: [{ row: 'A', seat_number: 1 }],
  payment_method: 'vodafone',
  status: 'completed'
});

// تحميل التذاكر
await downloadMultipleTickets(ticketInfo);
```

---

## 6️⃣ معالجة الأخطاء

### رسائل الأخطاء الشاملة:

| الخطأ | السبب | الحل |
|------|------|-----|
| "الرجاء إدخال اسم صحيح" | اسم أقل من حرفين | أدخل اسم بـ 2+ أحرف |
| "رقم الهاتف غير صحيح" | صيغة خاطئة | استخدم: 01012345678 |
| "البريد الإلكتروني غير صحيح" | صيغة غير صحيحة | استخدم: user@example.com |
| "الرجاء اختيار مقاعد" | لم تختر مقاعد | اختر عدد المقاعد المطلوب |
| "خطأ في الاتصال" | قطع الإنترنت | تحقق من الاتصال |

---

## 7️⃣ الميزات الإضافية

✅ **دعم العربية والإنجليزية**
✅ **تصميم ريسبونسيف** (موبايل/ويب)
✅ **تحميل فوري للتذاكر**
✅ **تشفير البيانات** (Supabase Auth)
✅ **RLS Policies** (حماية الصفوف)
✅ **Animations** (motion/framer-motion)
✅ **Barcode فريد** لكل تذكرة
✅ **مشاركة WhatsApp** آلية

---

## 8️⃣ معلومات الاتصال

**اسم الحدث**: Cinema Event 2026
**الموقع**: Egypt
**الدعم**: 24/7

---

## 9️⃣ الخطوات القادمة (اختيارية)

- [ ] إضافة نظام الدفع الحقيقي (Stripe/PayPal)
- [ ] تحسين البحث عن المقاعد
- [ ] إضافة نظام تقييمات
- [ ] لوحة تحكم المسؤول
- [ ] تطبيق موبايل (React Native)
- [ ] إشعارات البريد الإلكتروني

---

## 🔟 ملاحظات مهمة

1. **المقاعد متحفوظة** بعد الحجز (لا تُفتح إلا بعد الدفع)
2. **البيانات مشفرة** في قاعدة البيانات
3. **التذاكر PDF** فريدة لكل مستخدم
4. **معرف الحجز** لا يتكرر أبداً
5. **الدعم العربي الكامل** في جميع الرسائل

---

**تم الإعداد بنجاح! النظام جاهز للعمل.** 🎉
